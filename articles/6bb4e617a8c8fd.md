---
title: "銀行・カードと連携された家計簿アプリを作ってみた"
emoji: "😽"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: ["Next", "React", "tailwind", "shadcnui","chartjs","TypeScript"]
published: false
---

# 1. 概要
先日このような動画が公開されており、やってみたくなったので記録として残しておきます。

<!-- https://youtu.be/PuOVqP_cjkE?si=f_aWBvqHh08VhICE -->

https://github.com/adrianhajdin/banking/tree/main

# 2. セットアップする

## 2.1. ディレクトリを作成する
   `jsm_banking`という名前でディレクトリを作成し、ターミナルを開きます。

## 2.2. chadcn/uiをインストールする

### 2.2.1. アプリケーションを作成する
  https://ui.shadcn.com/docs/installation/next

  上記のリンク先に記載されているコマンドをターミナルで叩いてインストールします。ここで、`my-app`とある部分は今回`./`とします。これで現在のディレクトリにファイルが作成されます。
  ```:terminal
  npx create-next-app@latest ./ --typescript --tailwind --eslint
  ```
  `src/`ディレクトリを使うか、`App Router`を使うか、デフォルトのインポートエイリアスをカスタマイズを使うか、と尋ねられるのでそれぞれ`No`, `Yes`, `NO`と回答し、`Success!`と出れば完了です。

  ```:terminal
  ✔ Would you like to use `src/` directory? … No
  ✔ Would you like to use App Router? (recommended) … Yes
  ✔ Would you like to customize the default import alias (@/*)? … No
  ~~~~~~
  ~~~~~~
  Success!
  ```

### 2.2.2. CLIを実行する
  次にinitコマンドでCLIからアプリケーションをセットアップします。
  ```:terminal
  npx shadcn-ui@latest init
  ```
  スタイル、ベースカラー、CSS variablesはどうするかと聞かれるので`Default`, `Slate`, `yes`と回答し、先ほどと同様に`Success!`と出れば完了です。
  ```:terminal
  ✔ Which style would you like to use? › Default
  ✔ Which color would you like to use as base color? › Slate
  ✔ Would you like to use CSS variables for colors? … yes
  ~~~~~~
  ~~~~~~
  Success!
  ```

### 2.2.3. Buttonコンポーネントをインストールしてみる
  以下のコマンドを実行します。
  `components/ui`に`button.tsx`が作成されていれば完了です。
  ```:terminal
  npx shadcn-ui@latest add button
  ~~~~~~
  ~~~~~~
  ✔ Done.
  ```

## 2.3. 不要なファイルを削除し、アセットを追加する
  以下のファイル・ディレクトリを削除します。
  - `app/favicon.ico`
  - `lib`
  - `public`
  - `tailwind.config.ts`

  次に以下のリンクからzipファイルである`assets`をダウンロード後解凍し、ルートディレクトリに配置します。

  https://drive.google.com/file/d/1TVhdnD97LajGsyaiNa6sDs-ap-z1oerA/view?usp=sharing

  以下のようにファイル・ディレクトリを移動し、最後に`assets`は削除します。以下のコマンドで一括操作できます。
  ```:terminal
  mv assets/app/globals.css app/globals.css
  mv assets/constants constants  
  mv assets/lib lib            
  mv assets/public public
  mv assets/types types  
  mv assets/tailwind.config.ts tailwind.config.ts
  rm -rf assets
  ```

  ### 2.3.1. tailwind.config.ts
  ここでは主に`color`が追加されており、これにより例えばテキストのカラーを指定する際に`text-[#d1fadf]`ではなく`text-success-100`で指定できるのでよりコードが直感的なものにわかりやすくなります。
  ```:tailwind.confg.ts
   extend: {
      colors: {
        fill: {
          1: "rgba(255, 255, 255, 0.10)",
        },
        bankGradient: "#0179FE",
        indigo: {
          500: "#6172F3",
          700: "#3538CD",
        },
        success: {
          25: "#F6FEF9",
          50: "#ECFDF3",
          100: "#D1FADF",
          600: "#039855",
          700: "#027A48",
          900: "#054F31",
        },
        ~~~~~~
        ~~~~~~
      }
   }
  ```

### 2.3.2. types/index.d.ts
  ここでは今回のアプリケーションで使用するための型が複数用意されています。
  ```:index.d.ts
  declare type SignUpParams = {
    firstName: string;
    lastName: string;
    address1: string;
    city: string;
    state: string;
    postalCode: string;
    dateOfBirth: string;
    ssn: string;
    email: string;
    password: string;
};
  ```

### 2.3.3. public/icons
  名前の通り使用するアイコンがたくさん入っています。

### 2.3.4. lib/utils.ts
  日付、金額、文字列等のフォーマットを整えるためのいろんな関数が入ってるみたいな感じ。chatGPT使って生成したらしい。
  ```:utils.ts
  export const formatDateTime = (dateString: Date) => {
    const dateTimeOptions: Intl.DateTimeFormatOptions = {
      weekday: "short", // abbreviated weekday name (e.g., 'Mon')
      month: "short", // abbreviated month name (e.g., 'Oct')
      day: "numeric", // numeric day of the month (e.g., '25')
      hour: "numeric", // numeric hour (e.g., '8')
      minute: "numeric", // numeric minute (e.g., '30')
      hour12: true, // use 12-hour clock (true) or 24-hour clock (false)
    };
    ~~~~~~
    ~~~~~~
  }
  ```

### 2.3.5. constants/index.ts
  サイドバーのリンクとか、スタイルとかに使用する定数が宣言されている。
  ```:index.ts
  export const sidebarLinks = [
  {
    imgURL: "/icons/home.svg",
    route: "/",
    label: "Home",
  },
  {
    imgURL: "/icons/dollar-circle.svg",
    route: "/my-banks",
    label: "My Banks",
  },
  {
    imgURL: "/icons/transaction.svg",
    route: "/transaction-history",
    label: "Transaction History",
  },
  {
    imgURL: "/icons/money-send.svg",
    route: "/payment-transfer",
    label: "Transfer Funds",
  },
];
~~~~~~
~~~~~~
  ```

### 2.3.6. app/globals.css
  taliwindをインポートしてスクロールバーの非表示や色、入力フォームのような頻出のスタイルを簡単にあてられるようにされているみたいです。
  ```:globals.css
  ~~~~~~
  ~~~~~~
  @layer utilities {
    .input-class {
      @apply text-16 placeholder:text-16 rounded-lg border border-gray-300 text-gray-900 placeholder:text-gray-500;
    }

    .sheet-content button {
      @apply focus:ring-0 focus-visible:ring-transparent focus:ring-offset-0 focus-visible:ring-offset-0 focus-visible:outline-none focus-visible:border-none !important;
    }
    ~~~~~~
    ~~~~~~
  }
  ~~~~~~
  ~~~~~~
  ```

## 2.4. メタデータを修正する
  追加で別のフォントをインポートして、`title`, `description`, `icons`を追加し、`className`でアプリケーションで使用するフォントを使えるようにしています。
  ```diff tsx:layout.tsx
  import type { Metadata } from "next";
  - import { Inter } from "next/font/google";
  + import { Inter, IBM_Plex_Serif } from "next/font/google";
  import "./globals.css";

  - const inter = Inter({ subsets: ["latin"] });
  + const inter = Inter({ subsets: ["latin"], variable: '--font-inter' });
  + const ibmPlexSerif = IBM_Plex_Serif({
  +   subsets: ["latin"],
  +   weight: ['400', '700'],
  +   variable: '--font-ibm-plex-serif'
  + });

  export const metadata: Metadata = {
  -   title: "Create Next App",
  -   description: "Generated by create next app",
  +   title: "Horizon",
  +   description: "Horizon is a modern banking platform for everyone.",
  +   icons: {
  +     icon: '/icons/logo.svg'
  +   }
  };

  export default function RootLayout({
    children,
  }: Readonly<{
    children: React.ReactNode;
  }>) {
    return (
      <html lang="en">
  -       <body className={inter.className}>{children}</body>
  +             <body className={`${inter.variable} ${ibmPlexSerif.variable}`}>{children}</body>
      </html>
    );
  }
  ```
  これでセットアップは終わりです。



# 3. ファイル・ディレクトリ構成

## 3.1. (root)

  以下のコマンドで`app/page.tsx`を削除して`app/(root)/page.tsx`と`app/(root)/layout.tsx`を作成します。
  これでこのrootグループにのみlayoutを適応させることができます。
```:terminal
  rm app/page.tsx
  mkdir app/"(root)"
  touch app/"(root)"/page.tsx 
  touch app/"(root)"/layout.tsx 
```

`page.tsx`を簡単に書いてみます。
```typescript:page.tsx
import React from 'react';

const Home = () => {
  return <div>Home</div>;
}

export default Home;
```

`app/(root)/layout.tsx`に以下のサイドバーを表示するためのコードを書きます。
```typescript:layout.tsx
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <main>
      SIDEBAR
      {children}
    </main>
  );
}
```

## 3.2. (auth)
以下のコマンドで`app/(auth)/layout.tsx`等のauthに関連するファイルを作成します。
```:terminal
  mkdir app/"(auth)"
  mkdir app/"(auth)"/sign-in
  mkdir app/"(auth)"/sign-up
  touch app/"(auth)"/layout.tsx
  touch app/"(auth)"/sign-in/page.tsx
  touch app/"(auth)"/sign-up/page.tsx
```

そこに以下のコードを書きます。
```typescript:layout.tsx
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <main>
      {children}
    </main>
  );
}
```

```typescript:sign-in/page.tsx
import React from "react";

const SignIn = () => {
  return <div>Sign In</div>;
};

export default SignIn;
```

```typescript:sign-up/page.tsx
import React from "react";

const SignUp = () => {
  return <div>Sign Up</div>;
};

export default SignUp;
```

最低限の記述を終えたのでここで一度`npm run dev`を実行してローカルで動作を確認してみます。
以下の画像のようにSIDEBARとHomeが出力されているはずです。
また、`/sign-in`, `/sign-up`にアクセスしても同様に`Sign In`, `Sign Up`の文字だけが出力されるはずです。
これにより、ホーム画面ではサイドバーが表示され、サインインやサインアップでは表示されないような構成が作成できたことがわかります。
http://localhost:3000
![](/images/6bb4e617a8c8fd/2024-05-11-16-08-34.png)
通常のディレクトリを作成すると`/root`や`/auth`が必要であるはずですが、ディレクトリ名に`()`をつけることで不要になります。そしてそのグループ(ディレクトリ)毎のレイアウトやページを作成することができるようになります。

実はここまでがもっとも退屈な内容で、ここからはレイアウトやサイドバーのモダンなUI、ウェルカムメッセージ、chart.jsを使用した簡単なグラフを実装していきます。

そしてこの辺りで一度githubにリポジトリを作成してコミットしておくことをおすすめします。


# 4. Home Page UI

ホームから作成していきます。

## 4.1. ダッシュボード
### 4.1.1. HeaderBox
  `(root)/page.tsx`を編集します。

```typescript:page.tsx
import React from 'react';

const Home = () => {
  return (
    <section className="home">
    <div className='home-content'>
      <header className='home-header'>
        Welcome, Adrian
      </header>
    </div>
  </section>
  );
}

export default Home;
```
  Tailwind CSSにはhomeというクラスは存在しませんが、今回追加したアセット(`globals.css`)の中に存在しているので適応されます。
  また今回はこのようなヘッダーは複数のページに存在するためコンポーネント化しておきます。

  `components/HeaderBox.tsx`を作成し、以下のように記述します。
  ```typescript:HeaderBox.tsx
import React from "react";

const HeaderBox = () => {
  return <div>Header Box</div>;
};

export default HeaderBox;
  ```

そして作成したコンポーネントを使用するとpage.tsxは以下のようになります。

```diff typescript:page.tsx
- import React from 'react';
+ import HeaderBox from '@/components/HeaderBox

const Home = () => {
  return (
    <section className="home">
    <div className='home-content'>
      <header className='home-header'>
-         Welcome, Adrian
+         <HeaderBox />
      </header>
    </div>
  </section>
  );
}

export default Home;
```

さらにページによって表示する内容は異なるため引数を渡す必要があります。
例えば、あいさつ、名前、ページの内容など

それを踏まえたコードが以下のようになります。

```diff typescript:page.tsx
import HeaderBox from '@/components/HeaderBox

const Home = () => {
+ const loggedIn = { firstName: 'Adrian' }
+ 
  return (
    <section className="home">
    <div className='home-content'>
      <header className='home-header'>
-          <HeaderBox />
+          <HeaderBox 
+             type="greeting"
+             title="Welcome"
+             user={loggedIn?.firstName || 'Guest'}
+             subtext="Access and manage your account and transactions efficiently."
+          />
      </header>
    </div>
  </section>
  );
}

export default Home;
```

`loggedIn`には`firstName`を持つオブジェクトが代入され、もしログインできていればそのユーザーのファーストネームを、できていなければGuestと表示するようになっています。

そうしたら次は`HeaderBox`で引数を受け取る必要があるので`HeaderBox.tsx`を以下のように変更します。

```diff typescript:HeaderBox.tsx
- import React from "react";
- 
- const HeaderBox = () => {
+ const HeaderBox = ({ type = "title", title, subtext, user }: HeaderBoxProps) => {
  return <div>Header Box</div>;
};

export default HeaderBox;
```

アセットに含まれていた`types/index.d.ts`に`HeaderBoxProps`が定義されており、これと引数を一致させています。

::::details HeaderBoxProps
```typescript:inde.d.ts
declare interface HeaderBoxProps {
  type?: "title" | "greeting";
  title: string;
  subtext: string;
  user?: string;
}
```
::::

それでは実装します。
`{type === "greeting" && (...)}`は条件付きレンダリングと呼ばれるもので、typeがgreetingだったら()内を表示します。

```diff typescript:HeaderBox.tsx
const HeaderBox = ({ type = "title", title, subtext, user }: HeaderBoxProps) => {
-   return <div>Header Box</div>;
+   return (
+     <div className="header-box">
+       <h1 className="header-box-title">
+         {title}
+         {type === "greeting" && (
+           <span className="text-bankGradient">
+             &nbsp;{user}
+           </span>)}
+       </h1>
+       <p className="header-box-subtext">{subtext}</p>
+     </div>
+   )
};

export default HeaderBox;
```

ブラウザではこのような表示になっています。
![](/images/6bb4e617a8c8fd/2024-05-11-18-35-06.png)

これでヘッダーボックスは終わりです。

### 4.1.2. TotalBalanceBox

まずはコンポーネントを作成します。

```:terminal
touch components/TotalBalanceBox.tsx
```

```typescript:TotalBalanceBox.tsx
import React from "react";

const TotalBalanceBox = () => {
  return <div>Total Balance Box</div>;
};

export default TotalBalanceBox;
```

次にpage.tsxにコンポーネントを配置し、引数も渡します。

```diff typescript:page.tsx
        <header className='home-header'>
          <HeaderBox
            type="greeting"
            title="Welcome"
            user={loggedIn?.firstName || 'Guest'}
            subtext="Access and manage your account and transactions efficiently."
          />

+          <TotalBalanceBox 
+             accounts={[]}
+             totalBanks={1}
+             totalCurrentBalance={1250.35}
+           />
        </header>

```

ではTotalBalanceBoxの実装をしますが、先にいくつかnpm installしておきます。

```:terminal
npm install query-string
npm install react-countup
```

コードは以下のように変更してください。

```typescript:TotalBalanceBox.tsx
import { formatAmount } from "@/lib/utils";
import CountUp from "react-countup";

const TotalBalanceBox = ({
  accounts = [], totalBanks, totalCurrentBalance
}: TotlaBalanceBoxProps) => {
  return (
    <section className="total-balance">
      <div className="total-balance-chart">
        {/* Doughnut chart */}
      </div>

      <div className="flex flex-col gap-6">
        <h2 className="header-2">
          Bank Accounts: {totalBanks}
        </h2>
        <div className="flex flex-col gap-2">
          <p className="tota-balance-label">
            Total Current Balance
          </p>

          <p className="tota-balance-amount flex-center gap-2">
            <CountUp end={100} />
            {formatAmount(totalCurrentBalance)}
          </p>
        </div>
      </div>
    </section>
  )
};

export default TotalBalanceBox;
```
formatAmountでアメリカドルに変換しています。

::::details formatAmount
```typescript:utils.ts
export function formatAmount(amount: number): string {
  const formatter = new Intl.NumberFormat("en-US", {
    style: "currency",
    currency: "USD",
    minimumFractionDigits: 2,
  });

  return formatter.format(amount);
}
```
::::

ここで以下のようなエラーが発生したはずです。
```
Unhandled Runtime Error
Error: useRef only works in Client Components. Add the "use client" directive at the top of the file to use it. Read more: https://nextjs.org/docs/messages/react-client-hook-in-server-component
```

useRefを用いるときは`use client`をつけなければならないようです。
CountUpの実装を見てもuseRefを使用していないように見えますが、こちらからは見えない部分で使用されているのです。
::::details CountUp
```typescript:CountUp.d.ts
import React, { CSSProperties, ReactNode, ComponentPropsWithoutRef } from 'react';
import { CallbackProps, CommonProps, RenderCounterProps } from './types';
export interface CountUpProps extends CommonProps, CallbackProps {
    className?: string;
    redraw?: boolean;
    children?: (props: RenderCounterProps) => ReactNode;
    style?: CSSProperties;
    preserveValue?: boolean;
    containerProps?: ComponentPropsWithoutRef<'span'>;
}
declare const CountUp: React.FC<CountUpProps>;
export default CountUp;
```
::::
しかし`use client`をつけるとサーバー側のレンダリングが行われなくなってしまいます。
よって**新しいコンポーネントを作成する**ことで解決します。

```diff typescript:TotalBalanceBox.tsx
  <div className="flex flex-col gap-6">
    <h2 className="header-2">
      Bank Accounts: {totalBanks}
    </h2>
    <div className="flex flex-col gap-2">
      <p className="tota-balance-label">
        Total Current Balance
      </p>

      <p className="tota-balance-amount flex-center gap-2">
-         <CountUp end={100} />
-         {formatAmount(totalCurrentBalance)}
+         <AnimatedCounter amount=
+             {totalCurrentBalance} />
      </p>
    </div>
  </div>
```

新しいファイルを作成します。

```:terminal
touch components/AnimatedCounter.tsx
```

```ts:AnimatedCounter.tsx
'use client';

import CountUp from 'react-countup';

const AnimatedCounter = ({ amount }: { amount: number }) => {
  return (
    <p className='w-full'>
      <CountUp
        decimals={2}
        decimal=','
        prefix='$'
        end={amount} />
    </p>
  )
};

export default AnimatedCounter;
```

エラーで指摘された通り`use client`を用いて、小数点第2位まで表示させてプレフィックスに$も付けています。

最後にドーナツチャートです。

まずは`npm install chart.js`でインストールします。
以下の記事を見るに、別のパッケージをインストールする必要があるようです。
https://react-chartjs-2.js.org/
ということで`npm install --save chart.js react-chartjs-2`でインストールします。

次にコンポーネントを作成します。
```:terminal
touch components/DoughnutChart.tsx
```

コードは以下の通りです。
ひとまずはdataをベタ書きしています。
cutoutは線の太さ、legendは凡例です。

```ts:DoughnutChart.tsx
"use client";

import { Chart as ChartJS, ArcElement, Tooltip, Legend } from "chart.js";
import { Doughnut } from "react-chartjs-2";

ChartJS.register(ArcElement, Tooltip, Legend);

const DoughnutChart = ({ accounts }: DoughnutChartProps) => {
  const data = {
    datasets: [
      {
        label: "Banks",
        data: [1250, 2500, 3750],
        backgroundColor: ['#0747b6', '#2265d8', '#2f91fa']
      }
    ],
    labels: ['Bank 1', 'Bank 2', 'Bank 3']
  }

  return <Doughnut
    data={data}
    options={{
      cutout: '60%',
      plugins: {
        legend: {
          display: false
        }
      }
    }}
  />
};

export default DoughnutChart;
```

最終的なTotalBalanceBox.tsxは以下のようになります。
ここでは`accounts`は使用されていませんが、今後使うのでそのまま残しておきます。

```ts:TotalBalanceBox.tsx
import AnimatedCounter from "./AnimatedCounter";
import DoughnutChart from "./DoughnutChart";

const TotalBalanceBox = ({
  accounts = [], totalBanks, totalCurrentBalance
}: TotlaBalanceBoxProps) => {
  return (
    <section className="total-balance">
      <div className="total-balance-chart">
        <DoughnutChart accounts={accounts} />
      </div>

      <div className="flex flex-col gap-6">
        <h2 className="header-2">
          Bank Accounts: {totalBanks}
        </h2>
        <div className="flex flex-col gap-2">
          <p className="tota-balance-label">
            Total Current Balance
          </p>

          <div className="tota-balance-amount flex-center gap-2">
            <AnimatedCounter amount=
              {totalCurrentBalance} />
          </div>
        </div>
      </div>
    </section>
  )
};

export default TotalBalanceBox;
```

表示は以下の画像のようになっています。
数字がカウントアップされ、チャートが表示されているのがわかります。
これでHome Page UIは以上です。

![](/images/6bb4e617a8c8fd/2024-05-11-21-16-48.png)

# 5. Sidebars

次はサイドバーの実装に入ります。

## 5.1. Left Sidebar

まずは左側のサイドバーです。
`(root)/layout.tsx`から編集し、まずはコンポーネントを使用できる状態にします。

```diff ts:layout.tsx
+ import Sidebar from "@/components/Sidebar";
+ 
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
-     <main>
+     <main className="flex h-screen w-full font-inter">
      <Sidebar />
      {children}
    </main>
  );
}
```

次にサイドバーのコンポーネントを作成します。

```:terminal
touch components/Sidebar.tsx
```

```ts:Sidebar.tsx
import React from "react";

const Sidebar = () => {
  return <div>Sidebar</div>;
};

export default Sidebar;
```

これでとりあえずインポートはできるようになっているはずです。
では実装に取り掛かります。
`loggedIn`は将来的に自動的にデータを取得できるようになります。

```diff ts:layout.tsx
+ const loggedIn = { firstName: 'Adrian', lastName: 'JSM };
+ 
return (
  <main className="flex h-screen w-full font-inter">
-     <Sidebar />
+     <Sidebar user={loggedIn} />
    {children}
  </main>
);
```

`user`を渡しているので、サイドバーコンポーネントで受け取れるようにしてあげます。

```ts:Sidebar.tsx
import Link from "next/link";
import Image from "next/image";

const Sidebar = ({ user }: SiderbarProps) => {
  return (
    <section className="sidebar">
      <nav className="flex flex-col gap-4">
        <Link href="/"
          className="mb-12 cursor-pointer items-center gap-2">
          <Image
            src="/icons/logo.svg"
            width={34}
            height={34}
            alt="Horizon logo"
            className="size-[24px]
            max-xl:size-14"
          />
          <h1 className="sidebar-logo">Horizon</h1>
        </Link>
      </nav>
    </section>
  );
};

export default Sidebar;
```

`SidebarProps`にはあらかじめ用意されていた`User`が定義されています。

::::details SidebarProps
```ts:index.d.ts
declare interface SiderbarProps {
  user: User;
}

declare type User = {
  $id: string;
  email: string;
  userId: string;
  dwollaCustomerUrl: string;
  dwollaCustomerId: string;
  firstName: string;
  lastName: string;
  address1: string;
  city: string;
  state: string;
  postalCode: string;
  dateOfBirth: string;
  ssn: string;
};
```
::::

表示は以下のようになっています。ウィンドウの横幅を狭めると自動的にサイドバーは非表示になるはずです。

![](/images/6bb4e617a8c8fd/2024-05-12-13-48-48.png)

  では次に、このアプリケーションのすべてのページのリンクを作成していきます。`sidebarLinks`でマッピングすることによって実現します。

  `sidebarLinks`はアイコンやルーティングの宣言を5つの配列で行なっているようです。

::::details sidebarLinks
```ts:constants/index.ts
export const sidebarLinks = [
  {
    imgURL: "/icons/home.svg",
    route: "/",
    label: "Home",
  },
  {
    imgURL: "/icons/dollar-circle.svg",
    route: "/my-banks",
    label: "My Banks",
  },
  {
    imgURL: "/icons/transaction.svg",
    route: "/transaction-history",
    label: "Transaction History",
  },
  {
    imgURL: "/icons/money-send.svg",
    route: "/payment-transfer",
    label: "Transfer Funds",
  },
];
```
::::

```diff ts:Sidebar.tsx
import Link from "next/link";
import Image from "next/image";
+ import { sidebarLinks } from "@/constants";

const Sidebar = ({ user }: SiderbarProps) => {
  return (
    <section className="sidebar">
      <nav className="flex flex-col gap-4">
        <Link href="/"
          className="mb-12 cursor-pointer items-center gap-2">
          <Image
            src="/icons/logo.svg"
            width={34}
            height={34}
            alt="Horizon logo"
            className="size-[24px]
            max-xl:size-14"
          />
          <h1 className="sidebar-logo">Horizon</h1>
        </Link>

+         {sidebarLinks.map((item) => {
+           return (
+             <Link href={item.route} key={item.label}>
+               {item.label}
+             </Link>
+           )
+         })}
      </nav>
    </section>
  );
};

export default Sidebar;
```

これでサイドバーに新たに複数のリンクが追加されたはずです。(文字色が白なので背景と同化してしまっています)

![](/images/6bb4e617a8c8fd/2024-05-12-14-00-37.png)

このままでは見えないのでスタイルを当てる必要があります。ただ、現在自分のいるページだけは他のリンクと違うスタイルが当てられることになるので、それを判別する必要があります。

では実装していきます。


```diff ts:Sidebar.tsx
+ 'use client';
+ 
import Link from "next/link";
import Image from "next/image";
import { sidebarLinks } from "@/constants";
+ import { cn } from "@/lib/utils"
+ import { usePathname } from "next/navigation";

const Sidebar = ({ user }: SiderbarProps) => {
+   const pathname = usePathname();
+ 
  return (
    <section className="sidebar">
      <nav className="flex flex-col gap-4">
        <Link href="/"
          className="mb-12 cursor-pointer items-center gap-2">
          <Image
            src="/icons/logo.svg"
            width={34}
            height={34}
            alt="Horizon logo"
            className="size-[24px]
            max-xl:size-14"
          />
          <h1 className="sidebar-logo">Horizon</h1>
        </Link>

        {sidebarLinks.map((item) => {
+           const isActive = pathname === item.route || pathname.startsWith(`${item.route}/`)
          return (
+             <Link href={item.route} key={item.label}
+               className={cn('sidebar-link', {'bg-bank-gradient': isActive})}
+             >
              {item.label}
            </Link>
          )
        })}
      </nav>
    </section>
  );
};

export default Sidebar;
```

`use client`は動作確認のため一時的に使用しています。
`isActive`は現在のパスとリンクのパスが一致、もしくはそのリンクのパスから開始されている場合は`true`となります。
そしてisActiveがtrueの時だけ背景色を他のリンクと別のものに設定しています。その条件を判断して実行しているのが`cn`関数です。

現在表示は以下のようになっています。
![](/images/6bb4e617a8c8fd/2024-05-12-14-30-46.png)

このままでは文字が白くて見えないのでスタイルを当てて見えるようにして、一緒にアイコンも表示されるようにします。

```diff ts:Sidebar.tsx
- <Link href="/"
-           className="mb-12 cursor-pointer items-center gap-2">
+ <Link href="/"
+           className="mb-12 cursor-pointer flex items-center gap-2">

-               {item.label}
+               <div className="relative size-6">
+                 <Image
+                   src={item.imgURL}
+                   alt={item.label}
+                   fill
+                   className={cn({'brightness-[3] invert-0': isActive
+                 })}
+                 />
+               </div>
+               <p className={cn('sidebar-label', {'!text-white': isActive})}>
+                 {item.label}
+              </p>
```

これで綺麗な表示にすることができました。

![](/images/6bb4e617a8c8fd/2024-05-12-15-32-46.png)

## 5.2. Mobile Nav
モバイル版ではサイドバーが非表示になるので、専用のナビゲーションを追加する必要があります。

まず`(root)/layout.tsx`を編集します。

```diff ts:layout.tsx
    <main className="flex h-screen w-full font-inter">
      <Sidebar user={loggedIn} />
+ 
+     <div className="flex size-full flex-col">
+       <div className="root-layout">
+         <Image src="/icons/logo.svg" width={30} height={30} alt="logo" />
+         <div>
+           <MobileNav user={loggedIn} />
+         </div>
+       </div>
+       {children}
+     </div>
+ 
-       {children}
    </main>
```

そしてモバイル用のコンポーネントを作成し、chadcn/uiの`sheet`を利用します。
https://ui.shadcn.com/docs/components/sheet

```:terminal
touch components/MobileNav.tsx
npx shadcn-ui@latest add sheet
```

```ts:MobileNav.tsx
'use client'

import {
  Sheet,
  SheetContent,
  SheetDescription,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet"
import Link from "next/link";
import Image from "next/image";
import { sidebarLinks } from "@/constants";
import { cn } from "@/lib/utils";
import { usePathname } from "next/navigation";

const MobileNav = ({ user }: MobileNavProps) => {
  const pathname = usePathname();

  return <section className="w-full max-w-[264px]">
    <Sheet>
      <SheetTrigger>
        <Image
          src="/icons/hamburger.svg"
          width={30}
          height={30}
          alt="menu"
          className="cursor-pointer"
        />
      </SheetTrigger>
      <SheetContent side="left">
        <Link href="/"
          className="mb-12 cursor-pointer flex items-center gap-2">
          <Image
            src="/icons/logo.svg"
            width={34}
            height={34}
            alt="Horizon logo"
            className="size-[24px]
            max-xl:size-14"
          />
          <h1 className="sidebar-logo">Horizon</h1>
        </Link>

        {sidebarLinks.map((item) => {
          const isActive = pathname === item.route || pathname.startsWith(`${item.route}/`)

          return (
            <Link href={item.route} key={item.label}
              className={cn('sidebar-link', { 'bg-bank-gradient': isActive })}
            >
              <div className="relative size-6">
                <Image
                  src={item.imgURL}
                  alt={item.label}
                  fill
                  className={cn({
                    'brightness-[3] invert-0': isActive
                  })}
                />
              </div>
              <p className={cn('sidebar-label', { '!text-white': isActive })}>
                {item.label}
              </p>
            </Link>
          )
        })}
      </SheetContent>
    </Sheet>
  </section>;
};

export default MobileNav;
```

`<SheetContent>`内部の実装はSidebarと同様です。
また、画面右上に表示されるハンバーガーメニューをクリックすると以下のような表示が現れるはずです。

![](/images/6bb4e617a8c8fd/2024-05-12-16-29-24.png)

このスタイルを少し編集すると以下のようになります。

```diff ts:MobileNav.tsx
'use client'

import {
  Sheet,
  SheetContent,
  SheetDescription,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
+   SheetClose,
} from "@/components/ui/sheet"
import Link from "next/link";
import Image from "next/image";
import { sidebarLinks } from "@/constants";
import { cn } from "@/lib/utils";
import { usePathname } from "next/navigation";

const MobileNav = ({ user }: MobileNavProps) => {
  const pathname = usePathname();

  return <section className="w-full max-w-[264px]">
    <Sheet>
      <SheetTrigger>
        <Image
          src="/icons/hamburger.svg"
          width={30}
          height={30}
          alt="menu"
          className="cursor-pointer"
        />
      </SheetTrigger>
-       <SheetContent side="left">
+       <SheetContent side="left" className="border-none bg-white">
        <Link href="/"
-           className="mb-12 cursor-pointer flex items-center gap-2">
+           className="cursor-pointer flex items-center gap-1 px-4">
          <Image
            src="/icons/logo.svg"
            width={34}
            height={34}
            alt="Horizon logo"
-             className="size-[24px]
-             max-xl:size-14"
          />
-           <h1 className="sidebar-logo">Horizon</h1>
+           <h1 className="text-26 font-ibm-plex-serif font-bold text-black-1">Horizon</h1>
        </Link>
+         <div className="mobilenav-sheet">
+           <SheetClose asChild>
+             <nav className="flex h-full flex-col gap-6 pt-16 text-white">
              {sidebarLinks.map((item) => {
                const isActive = pathname === item.route || pathname.startsWith(`${item.route}/`)

                return (
+                   <SheetClose asChild key={item.route}>
                    <Link href={item.route} key={item.label}
-                       className={cn('sidebar-link', { 'bg-bank-gradient': isActive })}
+                       className={cn('mobilenav-sheet_close w-full', { 'bg-bank-gradient': isActive })}
                    >
-                     <div className="relative size-6">
                      <Image
                        src={item.imgURL}
                        alt={item.label}
-                         fill
+                         width={20}
+                         height={20}
                        className={cn({
                          'brightness-[3] invert-0': isActive
                        })}
                      />
-                     	<p className={cn('sidebar-label', { '!text-white': isActive })}>
+                       <p className={cn('text-16 font-semibold text-black-2', { 'text-white': isActive })}>
                        {item.label}
                      </p>
                    </Link>
+                   </SheetClose>
                )
              })}
+             </nav>
+           </SheetClose>
+         </div>
      </SheetContent>
    </Sheet>

  </section>;
};

export default MobileNav;
```

最終的には以下の画像のような表示になります。またメニューを開いてバツボタン以外でも画面外をクリックして閉じたり、Homeに遷移した後も自動で閉じたりするようにもなっているはずです。とても綺麗なナビゲーションが完成しました。

![](/images/6bb4e617a8c8fd/2024-05-12-17-12-27.png)

## Right Sidebar

続いては右側のサイドバーを作成していきます。

まずはコンポーネントの作成からです。
```:terminal
touch components/RightSidebar.tsx
```

トランザクションや銀行等の引数を持っています。

```ts:RightSidebar.tsx
import React from "react";

const RightSidebar = ({ user, transactions, banks }: RightSidebarProps) => {
  return <aside className="right-sidebar">
    <section className="flex flex-col pb-8">
      <div className="profile-banner" />
    </section>
  </aside>;
};

export default RightSidebar;
```

また今回は**Layout.tsxではなく**`page.tsx`に配置します。理由としては、LeftSidebarと違ってこのページでしか利用されないからです。

```diff ts:page.tsx
  <section className="home">
    <div className='home-content'>
      <header className='home-header'>
        <HeaderBox
          type="greeting"
          title="Welcome"
          user={loggedIn?.firstName || 'Guest'}
          subtext="Access and manage your account and transactions efficiently."
        />

        <TotalBalanceBox
          accounts={[]}
          totalBanks={1}
          totalCurrentBalance={1250.35}
        />
      </header>
    </div>
+     <RightSidebar 
+       user={loggedIn}
+       transaction={[]}
+       banks={[]}
+     />
  </section>
```

表示としては以下の画像のようになっています。
右側にサイドバーが表示されています。

![](/images/6bb4e617a8c8fd/2024-05-12-17-44-41.png)

しかしこれはPCでしか表示されず、スマートフォンでは表示されないようになっています。
globals.cssでは以下のようにスタイルが当てられています。
```css:globals.css
  .right-sidebar {
    @apply no-scrollbar hidden h-screen max-h-screen flex-col border-l border-gray-200 xl:flex w-[355px] xl:overflow-y-scroll !important;
  }
```

`xl:flex`の部分で、`min-width: 1280px`と書かれているのでその幅までは非表示となります。

次にカードを配置します。

```ts:RightSidebar.tsx
import Link from "next/link";
import Image from "next/image";

const RightSidebar = ({ user, transactions, banks }: RightSidebarProps) => {
  return <aside className="right-sidebar">
    <section className="flex flex-col pb-8">
      <div className="profile-banner" />
      <div className="profile">
        <div className="profile-img">
          <span className="text-5xl font-bold text-blue-500">
            {user.firstName[0]}
          </span>
        </div>

        <div className="profile-details">
          <h1 className="profile-name">
            {user.firstName} {user.lastName}
          </h1>
          <p className="profile-email">
            {user.email}
          </p>
        </div>
      </div>
    </section>

    <section className="banks">
      <div className="flex w-full justify-between">
        <h2 className="header-2">My Banks</h2>
        <Link href="/" className="flex gap-2">
          <Image
            src="/icons/plus.svg"
            width={20}
            height={20}
            alt="plus"
          />
          <h2 className="text-14 font-semibold text-gray-600">
            Add Bank
          </h2>
        </Link>
      </div>
      {banks?.length > 0 && (
        <div className="relative flex flex-1 flex-col items-center justify-center gap-5">
          <div className="relative z-10">
            BANK CARD 1
          </div>
          {banks[1] && (
            <div className="absolute right-0 top-8 z-0 w-[90%]">
              BANK CARD 2
            </div>
          )}

        </div>
      )}
    </section>
  </aside>;
};

export default RightSidebar;
```

RightSidebarで用いた引数をpage.tsxから渡してあげます。

```diff ts:page.tsx
import HeaderBox from '@/components/HeaderBox';
+ import RightSidebar from '@/components/RightSidebar';
import TotalBalanceBox from '@/components/TotalBalanceBox';

const Home = () => {
-   const loggedIn = { firstName: 'Adrian' };
+   const loggedIn = { firstName: 'Adrian', lastName: 'JSM', email: 'contact@jsmastery.pro' };

  return (
    <section className="home">
      <div className='home-content'>
        <header className='home-header'>
          <HeaderBox
            type="greeting"
            title="Welcome"
            user={loggedIn?.firstName || 'Guest'}
            subtext="Access and manage your account and transactions efficiently."
          />

          <TotalBalanceBox
            accounts={[]}
            totalBanks={1}
            totalCurrentBalance={1250.35}
          />
        </header>
      </div>
      <RightSidebar 
        user={loggedIn}
        transaction={[]}
-         banks={[]}
+         banks={[{}, {}]}
      />
    </section>
  );
}

export default Home;
```

表示は以下のようになっています。カードの1枚目と2枚目がズレて並んでいるようなレイアウトです。

![](/images/6bb4e617a8c8fd/2024-05-12-18-33-49.png)

では次はカードのコンポーネントを作成します。

```:terminal
touch components/BankCard.tsx
```

とりあえず表示させるためのコードを書きます。

```ts:BankCard.tsx
import React from "react";

const BankCard = () => {
  return <div>Bank Card</div>
}

export default BankCard;
```

そしてコンポーネントを配置します。ここではkeyやaccount等を渡しています。

```diff ts:RightSidebar.tsx
      {banks?.length > 0 && (
        <div className="relative flex flex-1 flex-col items-center justify-center gap-5">
          <div className="relative z-10">
-             BANK CARD 1
+             <BankCard
+               key={banks[0].$id}
+               account={banks[0]}
+               userName={`${user.firstName} ${user.lastName}`}
+               showBalance={false}
+             />
          </div>
          {banks[1] && (
            <div className="absolute right-0 top-8 z-0 w-[90%]">
-             BANK CARD 2
+               <BankCard
+                 key={banks[1].$id
+                 }
+                 account={banks[1]}
+                 userName={`${user.firstName} ${user.lastName}`}
+                 showBalance={false}
+               />
            </div>
          )}
        </div>
      )}
```

では`BankCard`の実装をしていきます。

まずはカードの外型と金額の表示までです。

```ts:BankCard.tsx
import { formatAmount } from "@/lib/utils";
import Link from "next/link";

const BankCard = ({ account, userName, showBalance = true }: CreditCardProps) => {
  return (
    <div className="flex flex-col">
      <Link href="/" className="bank-card">
        <div className="bank-card_content">
          <div >
            <h1 className="text-16 font-semibold text-white">
              {account.name || userName}
            </h1>
            <p className="font-ibm-plex-serif font-balck text-white">
              {formatAmount(account.currentBalance)}
            </p>
          </div>
        </div>
      </Link>
    </div>
  )
}

export default BankCard;
```

あとはpage.tsxから`currentBalance`を渡してあげます。

```diff ts:page.tsx
      <RightSidebar 
        user={loggedIn}
        transaction={[]}
-         banks={[{}, {}]}
+         banks={[{ currentBalance: 123.50 }, { currentBalance: 500.50 }]}
      />
```

するとこのように金額が表示されているのがわかります。

![](/images/6bb4e617a8c8fd/2024-05-12-19-51-21.png)

このまま続けてその他に記載する情報を表示していきます。

```diff ts:BankCard.tsx
import { formatAmount } from "@/lib/utils";
import Link from "next/link";
+ import Image from "next/image";

const BankCard = ({ account, userName, showBalance = true }: CreditCardProps) => {
  return (
    <div className="flex flex-col">
      <Link href="/" className="bank-card">
        <div className="bank-card_content">
          <div >
            <h1 className="text-16 font-semibold text-white">
              {account.name || userName}
            </h1>
            <p className="font-ibm-plex-serif font-balck text-white">
              {formatAmount(account.currentBalance)}
            </p>
          </div>
+ 
+           <article className="flex flex-col gap-2">
+             <div className="flex justify-between">
+               <h1 className="text-12 font-semibold text-white">
+                 {userName}
+               </h1>
+               <h2 className="text-12 font-semibold text-white">
+                 ●● / ●●
+               </h2>
+             </div>
+             <p className="text-14 font-semibold tracking-[1.1px] text-white">
+               ●●●● ●●●● ●●●● <span className="text-16">{account.mask}</span>
+             </p>
+           </article>
        </div>
+ 
-       <div>
+         <div className="bank-card_icon">
+           <Image
+             src="/icons/Paypass.svg"
+             width={20}
+             height={40}
+             alt="pay"
+           />
+           <Image
+             src="/icons/mastercard.svg"
+             width={45}
+             height={32}
+             alt="mastercard"
+             className="ml-5"
+           />
        </div>
+ 
+         <Image 
+           src="/icons/lines.png"
+           width={316}
+           height={190}
+           alt="lines"
+           className="absolute top-0 left-0"
+         />
      </Link>
    </div>
  )
}

export default BankCard;
```

これで有効期限や番号等の情報が表示されるようになりました。

![](/images/6bb4e617a8c8fd/2024-05-12-20-17-05.png)

これでサイドバーの実装は終了です。
